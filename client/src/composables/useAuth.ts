const useAuth = () => {
  // const api = import.meta.env.VITE_API;

  // @ts-ignore
  const login = (username: string, password: string) => {
  }

  // @ts-ignore
  const register = (data: {}) => {
  }

  return { login, register }
}

export { useAuth };
